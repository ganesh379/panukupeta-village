rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /families/{familyId} {
      allow read: if true;
      allow create: if request.resource.data.firstName != null;
      allow update, delete: if false;
    }
  }
}
